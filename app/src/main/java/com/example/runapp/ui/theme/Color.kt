package com.example.runapp.ui.theme

import androidx.compose.ui.graphics.Color

// New Blue Palette
val PrimaryBlue = Color(0xFF1976D2) // A strong, standard blue
val SecondaryBlue = Color(0xFF42A5F5) // Lighter blue for accents/gradients
val DarkBlueText = Color(0xFF0D47A1) // For text on light blue backgrounds

// Neutrals
val White = Color(0xFFFFFFFF)
val NearWhite = Color(0xFFF5F7FA) // Very light gray for backgrounds so cards stand out
val Black = Color(0xFF000000)
val DarkGray = Color(0xFF2C2C2E) // Softer black for main text
val MediumGray = Color(0xFF8E8E93) // For secondary text
val LightGrayDivider = Color(0xFFE5E5EA)

// Functional
val RedError = Color(0xFFD32F2F)
val GreenSuccess = Color(0xFF388E3C)

// Header Gradient Colors
val HeaderBlueStart = Color(0xFF42A5F5)
val HeaderBlueEnd = Color(0xFF1565C0)